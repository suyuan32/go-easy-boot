syntax = "v1"

info(
	title: "system service"
	desc: "this is the api discribe file for system services"
	author: "ryansu"
	email: "yuansu.china.work@gmail.com"
	version: "v1.0"
)

import "role.api"
import "user.api"
import "menu.api"
//import "base.api"
import "captcha.api"

@server(
	jwt: Auth
	group: system
	middleware: Cros
)

service system{
	@doc "system"
	@handler healthCheckHandler
	get /system/health
}